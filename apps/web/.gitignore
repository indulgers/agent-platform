# Track src/routeTree.gen.ts in git — generated by @tanstack/router-plugin
# during vite dev/build, but committing it means typecheck / Docker builds
# don't need to run vite first. Update by running `pnpm dev` once after
# adding/renaming a route, then commit the change.
